# Copy to frontend/.env and fill in. Placeholders only — never commit real values.
# Note: frontend/ is unmaintained scaffold (see root CLAUDE.md); it reads only this variable.
REACT_APP_BACKEND_URL=https://your-backend-url.com
